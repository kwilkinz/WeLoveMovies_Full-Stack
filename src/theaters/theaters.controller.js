const service = require("./theaters.service");
const asyncErrorBoundary = require("../errors/asyncErrorBoundary");

//TODO: ListBy Movie (movieId) > wait for the service to run listbymovies 
//TOdod so you can return them ( if no movieId then .list())

module.exports = {
    
}
